# Notion 设置

1. 在 Notion 建立 Internal Integration。
2. 把 Integration 连接到目标 Content Database。
3. 复制 Integration Token 到 `.env` 的 `NOTION_TOKEN`。
4. 复制 Database ID 到 `NOTION_CONTENT_DATABASE_ID`。
5. Database 属性必须包括 Name、Status、Platform、Publish Date。
